# CURD-using-File-System-in-Nodejs
A CRUD application built using Node.js and the file system module, utilizing a mock JSON file for data storage. It supports creating, reading, updating, and deleting records, demonstrating server-side logic, file handling, and basic data persistence without a database.
